{
  "project": "$VITE_FORMSPREE_PROJECT_ID",
  "forms": {
    "contactForm": {
      "name": "Contact Form",
      "fields": {
        "name": { "type": "text", "required": true },
        "email": { "type": "email", "required": true },
        "message": { "type": "text", "required": true }
      },
      "actions": [{ "type": "email", "to": "$VITE_CONTACT_EMAIL" }]
    }
  }
}
